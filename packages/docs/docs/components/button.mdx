---
sidebar_position: 2
title: Button
---
import { Button } from '../../../core/src';
import Container from '../../src/components/Container';
import { BiSearch, BiChevronRight } from 'react-icons/bi';
import PropsTable from '../../src/components/PropsTable';

<Container
    components={<>
        <Button variant={"Primary"}>Primary</Button>
        <Button variant={"Secondary"}>Secondary</Button>
        <Button variant={"Invisible"}>Invisible</Button>
    </>}
>

```jsx
<Button variant={"Primary"}>Primary</Button>
<Button variant={"Secondary"}>Secondary</Button>
<Button variant={"Invisible"}>Invisible</Button>
```

</Container>

## Props

<PropsTable 
    data={[
        {
            name: "leadingVisual",
            isRequired: false,
            type: "React.ReactElement",
            description: "An element that will appear before the button's content",
            default: ""
        },
        {
            name: "leadingVisualProps",
            isRequired: false,
            type: "HTMLProps<T>",
            description: "Props that will be applied to the root component of the leading visual",
            default: ""
        },
        {
            name: "trailingVisual",
            isRequired: false,
            type: "React.ReactElement",
            description: "An element that will appear after the button's content",
            default: ""
        },
        {
            name: "trailingVisualProps",
            isRequired: false,
            type: "HTMLProps<T>",
            description: "Props that will be applied to the root component of the trailing visual",
            default: ""
        },
        {
            name: "size",
            isRequired: false,
            type: `'xs' | 'sm' | 'md' | 'lg'`,
            description: "Controls the relative size of the button",
            default: `"md"`
        },
        {
            name: "variant",
            isRequired: false,
            type: `'Primary' | 'Secondary' | 'Invisible'`,
            description: "Controls the look & feel of the button",
            default: `"Primary"`
        },
        {
            name: "as",
            isRequired: false,
            type: `T`,
            description: "Controls the root element that is used by the button. Can be an HTML element string or a React component.",
            default: `"button"`
        },
        {
            name: "selected",
            isRequired: false,
            type: `boolean`,
            description: "Sets whether or not the button is displayed as selected or not. Particularly useful in a `ButtonGroup`",
            default: ``
        }
    ]}
/>

> Native props are forwarded to the root component.

<br />

## Examples

### Sizes

<Container
    components={<>
        <Button size="xs">X Small</Button>
        <Button size="sm">Small</Button>
        <Button size="md">Medium</Button>
        <Button size="lg">Large</Button>
    </>}
>

```jsx
<Button size="xs">X Small</Button>
<Button size="sm">Small</Button>
<Button size="md">Medium</Button>
<Button size="lg">Large</Button>
```

</Container>

### As an _a_ tag

<Container
    components={<Button as="a" href="/" leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>This is a link</Button>}
>

```jsx
<Button as="a" href="/" leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>This is a link</Button>
```
</Container>

### Display Button

<Container
    components={(
        <Button
            leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}
            trailingVisual={
                <BiChevronRight
                    size={"1.4em"}
                    color="inherit"
                />
            }
            trailingVisualProps={{ style: { marginLeft: "auto" } }}
            style={{ minWidth: 400 }}
        >
            Next Page
        </Button>
    )}
>

```jsx
<Button
    className='w-full'
    leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}
    trailingVisual={
        <BiChevronRight
            size={"1.4em"}
            color="inherit"
        />
    }
    trailingVisualProps={{ className: "ml-auto" }}
>
    Next Page
</Button>
```

</Container>


### With a counter


<Container
    components={(
        <>
            <Button trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
            <Button variant="Secondary" trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
            <Button variant="Invisible" trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
        </>
    )}
>

```jsx
<Button trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
<Button variant="Secondary" trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
<Button variant="Invisible" trailingVisual={<Button.Counter>2</Button.Counter>}>Users</Button>
```

</Container>

## Button.Counter

### Props

<PropsTable 
    data={[
        {
            name: "children",
            isRequired: false,
            type: "string",
            description: "Sets the number to display. Cast a numbers to a string",
            default: ""
        },
    ]}
/>

> Native props are forwarded to the root component.