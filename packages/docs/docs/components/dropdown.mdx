---
sidebar_position: 14
title: Dropdown
---
import { Menu, Dropdown } from '../../../core/src';
import Container from '../../src/components/Container';
import { BiSearch, BiUser, BiAlarm, BiQuestionMark, BiChevronRight } from 'react-icons/bi';
import PropsTable from '../../src/components/PropsTable';

<Container
    allowOverflow
    components={<>
        <Dropdown label='Choose an option'>
            <Menu.Item
                leadingVisual={<BiUser color='inherit' fontSize={"1.1em"} />}
                trailingVisual={"⌘ + P"}
                as="div"
            >
                Your Profile
            </Menu.Item>
            <Menu.Item
                leadingVisual={<BiAlarm color='inherit' fontSize={"1.1em"} />}
                trailingVisual={"⌘ + S"}
                as="div"
            >
                Account Settings
            </Menu.Item>
            <Menu.Divider isRowDivider />
            <Menu.Item as="div" variant='dangerous' selected trailingVisual={<BiChevronRight color='inherit' fontSize={"1.2em"} />}>Refer a Friend</Menu.Item>
            <Menu.Divider />
            <Menu.Item as="div" variant='dangerous'>Sign Out</Menu.Item>
        </Dropdown>
    </>}
>

```jsx
<Dropdown label='Choose an option'>
    <Menu.Item
        leadingVisual={<BiUser color='inherit' fontSize={"1.1em"} />}
        trailingVisual={"⌘ + P"}
        as="div"
    >
        Your Profile
    </Menu.Item>
    <Menu.Item
        leadingVisual={<BiAlarm color='inherit' fontSize={"1.1em"} />}
        trailingVisual={"⌘ + S"}
        as="div"
    >
        Account Settings
    </Menu.Item>
    <Menu.Divider isRowDivider />
    <Menu.Item as="div" variant='dangerous' selected trailingVisual={<BiChevronRight color='inherit' fontSize={"1.2em"} />}>Refer a Friend</Menu.Item>
    <Menu.Divider />
    <Menu.Item as="div" variant='dangerous'>Sign Out</Menu.Item>
</Dropdown>
```

</Container>

## Props

<PropsTable
    data={[
        {
            name: "justify",
            isRequired: false,
            type: "'left' | 'right'",
            description: "Controls the side that the dropdown is justified",
            default: '"right"'
        },
        {
            name: "align",
            isRequired: false,
            type: "'top' | 'bottom'",
            description: "Controls the side that the dropdown is aligned",
            default: '"bottom"'
        },
        {
            name: "customTrigger",
            isRequired: false,
            type: "React.ReactElement",
            description: "Controls the anchor of the dropdown",
            default: ''
        },
        {
            name: "triggerProps",
            isRequired: false,
            type: "IButtonProps<'button'>",
            description: "Props that are passed to the default anchor of the dropdown. Only applies if a no custom trigger is applied.",
            default: ''
        },
        {
            name: "label",
            isRequired: false,
            type: "string",
            description: "Label of the anchor of the dropdown. Only applies if no custom trigger is applied.",
            default: ''
        },
        {
            name: "menuProps",
            isRequired: false,
            type: 'HTMLProps<"ul">',
            description: "Props that are applied to the `<Menu />` of the dropdown.",
            default: ''
        },
        {
            name: "itemRootProps",
            isRequired: false,
            type: 'HTMLProps<"li">',
            description: "Props that are applied to the root `<Menu.Item />` of every item.",
            default: ''
        },
    ]}
/>

## Examples

### Dropdown above

<Container
    allowOverflow
    components={<>
        <Dropdown label='Choose an option' justify='left' align='top'>
            <Menu.Item
                leadingVisual={<BiUser color='inherit' fontSize={"1.1em"} />}
                trailingVisual={"⌘ + P"}
                as="div"
            >
                Your Profile
            </Menu.Item>
            <Menu.Item
                leadingVisual={<BiAlarm color='inherit' fontSize={"1.1em"} />}
                trailingVisual={"⌘ + S"}
                as="div"
            >
                Account Settings
            </Menu.Item>
            <Menu.Divider isRowDivider />
            <Menu.Item as="div" variant='dangerous' selected trailingVisual={<BiChevronRight color='inherit' fontSize={"1.2em"} />}>Refer a Friend</Menu.Item>
            <Menu.Divider />
            <Menu.Item as="div" variant='dangerous'>Sign Out</Menu.Item>
        </Dropdown>
    </>}
>

```jsx
<Dropdown label='Choose an option' justify='left' align='top'>
    <Menu.Item
        leadingVisual={<BiUser color='inherit' fontSize={"1.1em"} />}
        trailingVisual={"⌘ + P"}
        as="div"
    >
        Your Profile
    </Menu.Item>
    <Menu.Item
        leadingVisual={<BiAlarm color='inherit' fontSize={"1.1em"} />}
        trailingVisual={"⌘ + S"}
        as="div"
    >
        Account Settings
    </Menu.Item>
    <Menu.Divider isRowDivider />
    <Menu.Item as="div" variant='dangerous' selected trailingVisual={<BiChevronRight color='inherit' fontSize={"1.2em"} />}>Refer a Friend</Menu.Item>
    <Menu.Divider />
    <Menu.Item as="div" variant='dangerous'>Sign Out</Menu.Item>
</Dropdown>
```

</Container>
