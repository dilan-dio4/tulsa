---
sidebar_position: 12
title: ButtonGroup
---
import { ButtonGroup, Button, IconButton } from '../../../core/src';
import Container from '../../src/components/Container';
import { BiSearch } from 'react-icons/bi';

<Container
    vertical
    components={<>
        <ButtonGroup>
            <Button leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Unselected</Button>
            <Button selected leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Selected</Button>
            <Button leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Unselected</Button>
        </ButtonGroup>
    </>}
>

```jsx
<ButtonGroup>
    <Button leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Unselected</Button>
    <Button selected leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Selected</Button>
    <Button leadingVisual={<BiSearch size={"1.1em"} color="inherit" />}>Unselected</Button>
</ButtonGroup>
```

</Container>

## Props

`HTMLProps<"div">`

## Examples

### IconButtons

<Container
    vertical
    components={<>
        <ButtonGroup>
            <IconButton icon={<BiSearch size={"1em"} color="inherit" />} />
            <IconButton selected icon={<BiSearch size={"1em"} color="inherit" />} />
            <IconButton icon={<BiSearch size={"1em"} color="inherit" />} />
        </ButtonGroup>
    </>}
>

```jsx
<ButtonGroup>
    <IconButton icon={<BiSearch size={"1em"} color="inherit" />} />
    <IconButton selected icon={<BiSearch size={"1em"} color="inherit" />} />
    <IconButton icon={<BiSearch size={"1em"} color="inherit" />} />
</ButtonGroup>
```

</Container>